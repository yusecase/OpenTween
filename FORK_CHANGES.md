# このフォークの変更

基点: opentween/OpenTween の `8a07d2688c959b26b1eccecc5fb6db001338df6f`。
本家の履歴は CHANGELOG.txt に保持しています。この文書は個人改修部分のみを説明します。

## PublicSearch

- 保存済みCookieがあるTwitter/Xアカウントは、非表示WebView2でXの「最新」検索を開き、SearchTimelineの応答を取得します。
- 保存済みのauth_token/ct0を設定し、既存GraphQLパーサーで投稿に変換します。
- 設定件数までスクロールしてページを追加取得し、投稿IDで重複を除外します。
- Cookieがない場合、またはWebView2が失敗した場合は従来の直接GraphQL要求を使用します。予備経路の成功を保証するものではありません。
- 新しいユーザー情報形式（core/avatar等）の解析と、直接検索のパラメーターを調整しました。
- 高度な調査用に直接検索のquery IDを環境変数 `OPENTWEEN_TWITTER_QID_SEARCH_TIMELINE` で指定できます。これは認証情報ではなく、通常利用では設定不要です。

制限:

- 非公開のWeb画面・通信形式に依存します。X側の変更、認証、レート制限で動作しなくなる可能性があります。
- ブラウザ側のカーソルは保持せず、毎回先頭から収集します。「前データの取得」は本来のページ送りと同等ではありません。
- 更新間に取得範囲を超えた投稿は取りこぼす可能性があります。
- タイムアウトは45秒。途中まで取得できた場合はその結果を返すことがあります。200以外の応答がタイムアウトとして見える場合もあります。
- 検索ごとにWebView2を作成し、キャッシュ無効・スクロール間隔1.2秒を使用します。常駐化などの速度改善は撤回済みです。
- ローカルで検索成功とタイムアウトの両方を確認しています。障害原因は確定していません。

## 検知順

PublicSearchタブの右クリック「検知順で表示」で切り替えます。
後の更新で初めて取得した投稿を上に、同じ更新内では投稿日時順に表示します（降順時）。
再取得した同じIDは初回の順序を維持します。日時欄の値は元の投稿日時です。
日時列クリックで昇降順が切り替わり、他の列でソートすると検知順は解除されます。
設定は保存しますが検知履歴そのものは保存せず、再起動やタブ内容クリア後に再構築します。
Recent・リストにはこのソートを追加していません。

## いいね・リポスト数

- GraphQL応答から反応数を読み取り、一覧にいいね数、投稿詳細にいいね・リポスト数を表示します。
- 全体設定と、タブ右クリック「いいね・リポスト数の表示」の3択（全体設定に従う／表示する／表示しない）があります。
- 既存タブは全体設定に従います。個別設定は即時反映し、保存・復元します。
- DM詳細には表示しません。非表示でも数値の取得は継続します。
- 数値は取得時の情報で、独立したリアルタイム更新ではありません。

## 投稿用フォントの一括設定

フォント設定から一覧の既読・未読、投稿詳細、投稿入力に共通のフォントを適用できます。
個別設定の値は保持され、解除後は個別設定に戻ります。既存の強調スタイルを組み合わせます。

## 全件既読

タブの右クリック「このタブを全て既読」を追加しました。
既存の既読共有処理を使用するため、同じ投稿が他タブにある場合はそちらも既読になることがあります。

## 公開準備上の変更

- テスト用の追加ユーザー情報を架空の値に変更しました。
- 個人設定・実行用フォルダー・検証PoC・SDK等を除外しました。
- GitHub Actionsのテストとパッケージ生成は手動起動のみです。Codecov送信は無効にしました。
- 初回公開はソースのみです。GitHub Releasesへの自動公開はありません。

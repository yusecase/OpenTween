# 開発ガイド

このリポジトリは OpenTween の非公式な個人改修版です。本家の履歴・著作権表記・ライセンスを維持してください。

## 設定と認証情報

- `Setting*.xml`、Cookie、トークン、ログ、HAR、WebView2プロファイルをコミットしないでください。
- 動作確認で使う実行フォルダーを削除しないでください。ビルド成果物更新時は設定ファイルを除外します。
- テストデータは架空のユーザー情報を使い、実際の通信記録をそのまま追加しないでください。

## ビルド・検証

- Windows、Visual Studio の MSBuild、.NET Framework 4.8 開発ツールを使います。手順は README を参照してください。
- 通信・GraphQL変更時は GraphQL関連テスト、表示変更時は対応する表示・モデルテストを実行します。
- ビルド成功やパーサーテスト成功を、Xとの実通信成功と混同しないでください。
- WebView2変更は、初回・連続更新・タブ切り替え・失敗後の回復を実際の環境で確認します。未確認は明記します。
- 原因未確定の障害について推測を断定しないでください。

## 変更の扱い

- ユーザーの未コミット変更を破棄しないでください。
- PublicSearchは検索ごとにWebView2を作成し、キャッシュ無効、スクロール間隔1.2秒です。常駐化・高速化の実験は含みません。
- WebView2のアーキテクチャとローダーの一致を維持します。現状のAnyCPU構成は32ビット優先です。
- 変更は目的別に分け、公開前にソース差分と秘密情報の混入を確認してください。
- 実行ファイルの配布・GitHubへのpushは、依頼された範囲と承認条件に従ってください。
